# Machine-readable rendering of system/ENGINE_INTERFACE_CONTRACT.md §12.
# Non-authoritative: if this ever disagrees with that document, the
# Markdown document wins and this file is a bug. See schemas/README.md.
# schema_version bumped 1.0.0 -> 1.1.0: decision dropped EXECUTE (this
# engine never processes it, per PORTFOLIO_OPTIMIZATION_ENGINE.md §1) and
# capital_required was removed (no longer a valid field once EXECUTE was
# dropped) — both a field/enum retyping per §1A's schema_version rule.
$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "trx://schemas/portfolio_optimization.schema.yaml"
title: TRX Portfolio Optimization Engine Output
oneOf:
  - $ref: "#/$defs/success"
  - $ref: "#/$defs/failure"

$defs:
  failure:
    type: object
    additionalProperties: false
    required: [engine, engine_version, schema_version, failure_state]
    properties:
      engine: { const: "PORTFOLIO_OPTIMIZATION_ENGINE" }
      engine_version: { type: string }
      schema_version: { const: "1.1.0" }
      failure_state:
        type: object
        additionalProperties: false
        required: [category, reason]
        properties:
          category:
            type: string
            enum: [DATA_FAILURE, MODEL_FAILURE, CONFLICT_FAILURE, RISK_REJECTION, EXECUTION_INVALID, SYSTEM_ERROR]
          reason: { type: string }

  success:
    type: object
    additionalProperties: false
    required:
      - engine
      - engine_version
      - schema_version
      - ticker
      - decision
      - current_weight_pct
      - target_weight_pct
      - suggested_shares
      - capital_released
      - reason
      - priority
      - execution_required
      - status
    properties:
      engine: { const: "PORTFOLIO_OPTIMIZATION_ENGINE" }
      engine_version: { type: string }
      schema_version: { const: "1.1.0" }
      ticker: { type: string }
      decision:
        type: string
        enum: [HOLD, REDUCE, EXIT, WATCH]
        description: "Echoed verbatim from MASTER_DECISION_ENGINE.md §11 final_outcome for this ticker; never computed here. This engine never processes EXECUTE — see CAPITAL_ALLOCATION_ENGINE.md. A mismatch against Master Decision's value is a contract violation in this engine's output, not a newer or overriding value."
      current_weight_pct: { type: number, description: "Percent of portfolio value, from PORTFOLIO_ENGINE.md." }
      target_weight_pct: { type: number, description: "Percent of portfolio value, computed per PORTFOLIO_OPTIMIZATION_ENGINE.md §7." }
      suggested_shares:
        type: number
        maximum: 0
        description: "Signed share count, always <= 0 (a sell or no change) — this engine never produces a positive/buy share count. 0 for HOLD/WATCH unless an advisory note applies, per §7A."
      capital_released:
        type: [number, "null"]
        description: "Currency amount freed by a REDUCE/EXIT. null for HOLD/WATCH with no advisory."
      reason:
        type: string
        description: "Traces to the Master Decision rationale and any construction-limit rule applied, per §6."
      priority:
        type: string
        enum: [P1, P2, P3]
        description: "Per EXECUTION_ENGINE.md §3G."
      execution_required:
        type: boolean
        description: "false for HOLD/WATCH with no advisory triggered."
      status:
        type: string
        enum: ["READY", "PORTFOLIO OPTIMIZATION INCOMPLETE — DATA REQUIRED", "NO REBALANCING ACTION"]
